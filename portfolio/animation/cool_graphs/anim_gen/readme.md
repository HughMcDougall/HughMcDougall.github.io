To Use:
1. Use Padowan graph to save all images to this folder with the key "img ___"
2. run `_make.py`
3. Extract and rename the gif from this folder to the prev folder
4. run `crop.py` to save a thumbnail to `./anim_thumbs`
5. Move `anim_thumbs` to the `images/thumbs` folder in the home directory